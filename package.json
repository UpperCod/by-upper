{
  "name": "wordpress-base",
  "description": "v2",
  "version": "0.0.0",
  "scripts": {
    "start": "bundle src/*.{js,css} static --watch",
    "build": "bundle src/*.{js,css} static --minify"
  },
  "dependencies": {
    "atomico": "^0.20.0"
  },
  "devDependencies": {
    "bundle-cli": "^0.20.3"
  }
}
