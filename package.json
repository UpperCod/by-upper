{
  "name": "wordpress-base",
  "description": "v2",
  "version": "0.0.0",
  "scripts": {
    "start": "bundle src/*.{js,css} static --watch",
    "build": "bundle src/*.{js,css} static --minify"
  },
  "dependencies": {
    "@glidejs/glide": "^3.4.1",
    "atomico": "^0.23.2"
  },
  "devDependencies": {
    "bundle-cli": "^0.22.0"
  }
}
